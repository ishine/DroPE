defaults:
  - base
  - _self_

model_log_name: llama2v7b
model_name_or_path: meta-llama/Llama-2-7b-hf

model_args:
  model_revision: main
  torch_dtype: float16
  attn_implementation: flash_attention_2

tokenizer:
  model_revision: ${model_args.model_revision}

make_tokenizer_fn:
  force_override_pad_token: true
